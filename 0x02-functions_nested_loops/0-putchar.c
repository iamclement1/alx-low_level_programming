#include <stdio.h>
/**
 * main - The program should return 0
 *
 * Return: Always 0 (success)
 */
int main(void)
{	int sh = 0;	

	while (sh = 0)
	{
		putchar('\n');
		sh++;
	}

	putchar('\n');

	return (0);
}
